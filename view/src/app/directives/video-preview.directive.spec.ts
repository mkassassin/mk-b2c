import { VideoPreviewDirective } from './video-preview.directive';

describe('VideoPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
